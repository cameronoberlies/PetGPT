import React, { useState } from 'react';
import Breed from './Breed';





const Favorites = () => {
//   const [selectedBreeds, setSelectedBreeds] = useState([]);
//   const [favorites, setFavorites] = useState([]);

//   const handleCheckboxChange = (breed) => {
//     if (selectedBreeds.includes(breed)) {
//       setSelectedBreeds((prevSelected) => prevSelected.filter((selectedBreed) => selectedBreed !== breed));
//     } else {
//       setSelectedBreeds((prevSelected) => [...prevSelected, breed]);
//     }
//   };

//   const addToFavorites = (breed) => {
//     setFavorites((prevFavorites) => [...prevFavorites, breed]);
//   };

//   return (
//     <div>
//       <h2>Dog Breeds List</h2>
//       <Breed
//   dogBreeds={dogBreeds}
//   selectedBreeds={selectedBreeds}
//   handleCheckboxChange={handleCheckboxChange}
//   addToFavorites={addToFavorites}
// />
//       <h2>Favorites</h2>
//       <ul>
//         {favorites.map((favoriteBreed) => (
//           <li key={favoriteBreed}>{favoriteBreed}</li>
//         ))}
//       </ul>
//     </div>
//   );
};

export default Favorites;